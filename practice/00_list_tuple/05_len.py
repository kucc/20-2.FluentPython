import numpy as np

lst = [1, 2, 3]
tup = (1, 2, 3)
dct = {'a': 1, 'b': 2, 'c': 3}
np_arr = np.array([1, 2, 3])

print("len(lst): %d" % len(lst))
print("len(tup): %d" % len(tup))
print("len(dct): %d" % len(dct))
print("len(np_arr): %d" % len(np_arr))
